#include <iostream>
#include <string>
#include <vector>
#include <map>

using namespace std;
struct point {
    int x,y;
};
int main()
{
 
 map < point, string > names;
 point a;
 for(int i = 0; i < n; i++){
     cin >> a.x >> a.y;
     string name;
     cin >> name;
     names[a] = name;
     
 } 
 point b;
 cin >> b.x >> b.y;
 cout << names[b];
 
  
}
